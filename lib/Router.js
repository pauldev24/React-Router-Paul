import{jsx as _jsx}from"react/jsx-runtime";import{useEffect,useState}from"react";import{EVENTS}from"./const";import{match}from"path-to-regexp";import{Children}from"react";import{getCurrentPath}from"./utils";export function Router({children,routes=[],defaultComponent:DefaultComponent=()=>_jsx("h1",{children:"404"})}){const[currenPath,setCurrentPath]=useState(getCurrentPath);useEffect(()=>{const onLocationChange=()=>{setCurrentPath(getCurrentPath)};window.addEventListener(EVENTS.PUSHSTATE,onLocationChange);window.addEventListener(EVENTS.POPSTATE,onLocationChange);return()=>{window.removeEventListener(EVENTS.PUSHSTATE,onLocationChange);window.removeEventListener(EVENTS.POPSTATE,onLocationChange)}},[]);let routeParams={};const routesFromChildren=Children.map(children,({props,type})=>{const{name}=type;const isRoute=name=="Route";return isRoute?props:null});const routeToUse=routes.concat(routesFromChildren).filter(Boolean);const Page=routeToUse.find(({path})=>{if(path==currenPath)return true;const matchUrl=match(path,{decode:decodeURIComponent});const matched=matchUrl(currenPath);if(!matched)return false;routeParams=matched.params;return true})?.Component;return Page?_jsx(Page,{routeParams:routeParams}):_jsx(DefaultComponent,{routeParams:routeParams})}